# Phase 1-2 Result Artifacts

This folder preserves lightweight outputs from the individual transfer-learning notebook:

- train/validation curves
- confusion matrices
- prediction and misclassification summaries
- model comparison metrics

Classifier checkpoint files are not included because they are generated binaries. Recreate them by running the notebooks or training scripts.

