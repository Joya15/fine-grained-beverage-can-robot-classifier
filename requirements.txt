# COMP8430 Phase 3 — Python dependencies
# pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyyaml>=6.0
tqdm>=4.65.0

# Optional for laptop webcam YOLO-based ROI detection.
ultralytics>=8.0.0

# ROS2 deps (system packages on Ubuntu robot — do NOT pip install):
# ros-humble-cv-bridge
# python3-rclpy
